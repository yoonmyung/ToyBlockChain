using System;
using System.Collections.Generic;
using System.Text;

namespace PracticeBlockChain
{
    // Libplanet/BlockHeader.cs의 Validate함수 참고
    class Validation
    {
        // 1. previous hash가 올바른지
        // 2. difficulty보다 작은 값이 맞는지
        // (그외) previous timestamp와 currrent timestamp 차이가 조건보다 작은지
        // (조건보다 크면 너무 오래 전에 생긴 블록이므로 chain에 연결하지 않는다)

        // 등등의 validation 방법이 있다 
    }
}
